
# server {
#   listen 80;
#   server_name _;
#
#   root /usr/share/nginx/html;
#   index index.html;
#
#   # Serve static front-end
#   location / {
#     try_files $uri /index.html;
#   }
#
#   # Proxy API calls to the backend container by service name
#   location /api/ {
#     proxy_pass http://10.0.3.137:3000;  # use backend private IP here
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#   }
# }
#
server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Serve static front-end
  location / {
    try_files $uri /index.html;
  }

  # Don't proxy here — let the ALB route /api to backend target group.
  # Frontend JS should call relative /api/... on the same host.
}
